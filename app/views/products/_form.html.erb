<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
</head>

<body>
  <div class="container text-center">
      <%= form_for @product do |f| %>
        <% if @product.errors.any? %>	
      	 <div class="alert alert-danger" id="error_explanation">
      	   <h4>Error, no se permite guardar este producto debido a:</h4>
      	   <ul>
              <% @product.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
          </ul>
         </div>
        <% end %>
      <div class="col col-md-6 col-md-offset-3">

      <div class="form-group">
        <%= f.label :nombre %><br>
        <%= f.text_field :name, :class => "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :descripcion %><br>
        <%= f.text_field :description, :class => "form-control" %>
      </div>

      <div class="form-group">    
        <%= f.label "precio ($)" %><br>
        <%= f.text_field :price, :class => "form-control" %>
      </div>

      <div class="form-group">    
        <%= f.label :marca %><br>
        <%= f.collection_select :brand_id, Brand.all, :id, :name, :prompt => "Seleccione la marca", :class => "dropdown-menu" %>
      </div>
     
      <p>
        <%= f.submit 'Agregar producto',:class => "btn btn-success"  %>
      </p>

      <% end %>
      <%= link_to 'Atrás', products_path, :class => "btn btn-primary" %>
    </div>  
  </div>



  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</body>
